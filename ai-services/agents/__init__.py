"""AI Agent Layer for the Gasless Auction dApp.

The only public API is the orchestrator function:

    from agents.orchestrator import orchestrator
    result = await orchestrator({"type": "NEW_LISTING", ...})
"""
